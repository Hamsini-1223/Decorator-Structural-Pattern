{
  "name": "decorator-pattern-clothing",
  "version": "1.0.0",
  "description": "TypeScript implementation of the Decorator pattern using a clothing analogy",
  "main": "dist/demos/interactiveDemo.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/demos/interactiveDemo.js",
    "interactive": "npm run build && node dist/demos/interactiveDemo.js",
    "simple": "npm run build && node dist/demos/simpleDemo.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "decorator-pattern",
    "design-patterns",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}
